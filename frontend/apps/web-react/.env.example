# Base URL of the Portfolio API (versioned root).
# Leave relative — Vite proxies `/api` to the backend in dev (see vite.config.ts),
# and prod serves both from one origin. Set VITE_API_PROXY_TARGET to change the
# dev proxy target (default http://localhost:5050).
VITE_API_BASE_URL=/api/v1
