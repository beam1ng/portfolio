{
  "name": "portfolio",
  "version": "0.0.0",
  "private": true,
  "description": "Personal portfolio platform — ASP.NET Core API + React/TS frontend monorepo.",
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "start": "node scripts/dev.mjs",
    "stop": "docker compose down",
    "dev": "pnpm --filter web-react dev",
    "build": "pnpm -r build",
    "build:static": "pnpm --filter web-react build:static",
    "export:content": "node scripts/export-content.mjs",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
